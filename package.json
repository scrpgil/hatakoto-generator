{
  "name": "hatakoto-generator",
  "private": true,
  "version": "0.0.1",
  "description": "hatakoto-generator",
  "license": "MIT",
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "stencil build && cp src/_redirects www/",
    "start": "stencil build --dev --watch --serve",
    "test": "stencil test --spec --e2e",
    "test.watch": "stencil test --spec --e2e --watch"
  },
  "dependencies": {
    "@ionic/core": "one",
    "@stencil/sass": "^1.0.0"
  },
  "devDependencies": {
    "@stencil/core": "1.0.0-beta.8"
  }
}
